'use strict';const express=require("express"),app=express(),User=require("../../models/User"),brcypt=require("bcryptjs"),router=express.Router();router.all("/*",(a,b,c)=>{a.app.locals.layout="logs";c()});router.get("/sign",(a,b)=>{b.render("home/logs")});router.post("/login",(a,b)=>{b.send("receiving response....")});router.post("/register",(a,b)=>{User.findOne({email:a.body.email}).then(c=>{c?(a.flash("error_msg","User exists with email"),b.redirect("back")):new User})});module.exports=router;