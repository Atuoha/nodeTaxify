'use strict';const mongoose=require("mongoose"),Schema=mongoose.Schema,CategorySchema=new Schema({title:{type:String},date:{type:Date,default:new Date}});module.exports=mongoose.model("categories",CategorySchema);